import { Translations } from '../../types';

const notFound: Translations['notFound'] = {
    tag: 'No pude encontrar la página que estabas buscando, haz clic aquí para ir a casa.'
};

export default notFound;
