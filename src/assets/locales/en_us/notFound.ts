import { Translations } from '../../types';

const notFound: Translations['notFound'] = {
    tag: "I couldn't find the page you were looking for, click here to go home."
};

export default notFound;
