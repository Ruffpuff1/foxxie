import type { NextPage } from 'next';
import { Nav } from '../components/Nav';

const Kettu: NextPage = () => {
    return (
        <div>
            <Nav name='kettu' />
        </div>
    );
};

export default Kettu;
